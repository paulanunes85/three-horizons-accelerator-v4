# =============================================================================
# THREE HORIZONS ACCELERATOR — Demo Secrets (Local Demo)
# =============================================================================
# WARNING: These are demo-only credentials. Never use in production.
# Values are base64 encoded versions of the passwords in local/config/local.env
# =============================================================================
apiVersion: v1
kind: Secret
metadata:
  name: demo-secrets
  namespace: databases
  labels:
    app.kubernetes.io/part-of: three-horizons
type: Opaque
stringData:
  postgres-password: "demo-postgres-2026"
  redis-password: "demo-redis-2026"
---
apiVersion: v1
kind: Secret
metadata:
  name: argocd-admin-secret
  namespace: argocd
  labels:
    app.kubernetes.io/part-of: three-horizons
type: Opaque
stringData:
  admin.password: "demo-argocd-2026"
