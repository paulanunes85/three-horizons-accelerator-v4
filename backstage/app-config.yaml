app:
  title: paulasilvatech — Agentic DevOps Platform (Open Horizons)
  baseUrl: http://localhost:3000

organization:
  name: paulasilvatech

backend:
  # Used for enabling authentication, secret is shared by all backend plugins
  # See https://backstage.io/docs/auth/service-to-service-auth for
  # information on the format
  # auth:
  #   keys:
  #     - secret: ${BACKEND_SECRET}
  baseUrl: http://localhost:7007
  listen:
    port: 7007
    # Uncomment the following host directive to bind to specific interfaces
    # host: 127.0.0.1
  csp:
    connect-src: ["'self'", 'http:', 'https:']
    # Content-Security-Policy directives follow the Helmet format: https://helmetjs.github.io/#reference
    # Default Helmet Content-Security-Policy values can be removed by setting the key to false
  cors:
    origin: http://localhost:3000
    methods: [GET, HEAD, PATCH, POST, PUT, DELETE]
    credentials: true
  # This is for local development only, it is not recommended to use this in production
  # The production database configuration is stored in app-config.production.yaml
  database:
    client: better-sqlite3
    connection: ':memory:'
  # workingDirectory: /tmp # Use this to configure a working directory for the scaffolder, defaults to the OS temp-dir

integrations:
  github:
    - host: github.com
      # This is a Personal Access Token or PAT from GitHub. You can find out how to generate this token, and more information
      # about setting up the GitHub integration here: https://backstage.io/docs/integrations/github/locations#configuration
      token: ${GITHUB_TOKEN}
    ### Example for how to add your GitHub Enterprise instance using the API:
    # - host: ghe.example.net
    #   apiBaseUrl: https://ghe.example.net/api/v3
    #   token: ${GHE_TOKEN}

proxy:
  endpoints:
    '/github/api':
      target: 'https://api.github.com'
      headers:
        Authorization: 'Bearer ${GITHUB_TOKEN}'
        Accept: 'application/vnd.github+json'
        X-GitHub-Api-Version: '2022-11-28'
      changeOrigin: true
      allowedMethods: ['GET']

# Reference documentation http://backstage.io/docs/features/techdocs/configuration
# Note: After experimenting with basic setup, use CI/CD to generate docs
# and an external cloud storage when deploying TechDocs for production use-case.
# https://backstage.io/docs/features/techdocs/how-to-guides#how-to-migrate-from-techdocs-basic-to-recommended-deployment-approach
techdocs:
  builder: 'local' # Alternatives - 'external'
  generator:
    runIn: 'docker' # Alternatives - 'local'
  publisher:
    type: 'local' # Alternatives - 'googleGcs' or 'awsS3'. Read documentation for using alternatives.

auth:
  # see https://backstage.io/docs/auth/ to learn about auth providers
  providers:
    # See https://backstage.io/docs/auth/guest/provider
    guest: {}

scaffolder:
  # see https://backstage.io/docs/features/software-templates/configuration for software template options

catalog:
  import:
    entityFilename: catalog-info.yaml
    pullRequestBranchName: backstage-integration
  rules:
    - allow: [Component, System, API, Resource, Location, Domain, Template, User, Group]
  locations:
    # -----------------------------------------------------------------
    # Platform Self-Registration
    # -----------------------------------------------------------------
    - type: file
      target: ../../catalog-info.yaml

    # -----------------------------------------------------------------
    # Organization Data (groups, users, domain, systems)
    # -----------------------------------------------------------------
    - type: file
      target: ../../examples/org.yaml
      rules:
        - allow: [User, Group, Domain, System]

    # -----------------------------------------------------------------
    # Platform Entities (components, APIs, resources)
    # -----------------------------------------------------------------
    - type: file
      target: ../../examples/entities.yaml

    # -----------------------------------------------------------------
    # H1 Foundation Golden Path Templates (6)
    # -----------------------------------------------------------------
    - type: file
      target: ../../../golden-paths/h1-foundation/web-application/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h1-foundation/new-microservice/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h1-foundation/basic-cicd/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h1-foundation/documentation-site/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h1-foundation/infrastructure-provisioning/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h1-foundation/security-baseline/template.yaml
      rules:
        - allow: [Template]

    # -----------------------------------------------------------------
    # H2 Enhancement Golden Path Templates (9)
    # -----------------------------------------------------------------
    - type: file
      target: ../../../golden-paths/h2-enhancement/api-microservice/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h2-enhancement/microservice/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h2-enhancement/api-gateway/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h2-enhancement/gitops-deployment/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h2-enhancement/event-driven-microservice/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h2-enhancement/batch-job/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h2-enhancement/data-pipeline/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h2-enhancement/reusable-workflows/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h2-enhancement/ado-to-github-migration/template.yaml
      rules:
        - allow: [Template]

    # -----------------------------------------------------------------
    # H3 Innovation Golden Path Templates (7)
    # -----------------------------------------------------------------
    - type: file
      target: ../../../golden-paths/h3-innovation/rag-application/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h3-innovation/copilot-extension/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h3-innovation/foundry-agent/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h3-innovation/multi-agent-system/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h3-innovation/mlops-pipeline/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h3-innovation/ai-evaluation-pipeline/template.yaml
      rules:
        - allow: [Template]
    - type: file
      target: ../../../golden-paths/h3-innovation/sre-agent-integration/template.yaml
      rules:
        - allow: [Template]

    # -----------------------------------------------------------------
    # Example Template (Backstage stock)
    # -----------------------------------------------------------------
    - type: file
      target: ../../examples/template/template.yaml
      rules:
        - allow: [Template]

kubernetes:
  # see https://backstage.io/docs/features/kubernetes/configuration for kubernetes configuration options

# see https://backstage.io/docs/permissions/getting-started for more on the permission framework
permission:
  # setting this to `false` will disable permissions
  enabled: true
